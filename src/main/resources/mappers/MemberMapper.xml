<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatic.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gati.hankki.member.mapper.MemberMapper">

    <select id="findByMemberId" resultType="com.gati.hankki.member.model.Member">
	    SELECT 
	        member_no,
	        member_id,
	        member_pass,
	        member_name,
	        member_address,
	        member_age,
	        out_yn,
	        del_yn,
	        auth_code,
	        auth_yn,
	        created_id,
	        created_at,
	        updated_id,
	        updated_at
	    FROM 
	    	member
	    WHERE 
	    	member_id = #{memberId}
	</select>

</mapper>