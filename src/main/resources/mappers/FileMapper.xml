<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatic.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gati.hankki.file.mapper.FileMapper">

    <insert id="insertFile" parameterType="com.gati.hankki.file.domain.FileMeta">
        INSERT INTO Files (
            BOARD_NO,
            FILE_PATH,
            ORIGINAL_FILE_NAME,
            REAL_FILE_NAME,
            FILE_EXTENSION,
            FILE_SIZE,
            CREATED_ID,
            CREATED_AT
        )
        VALUES (
            #{boardId},
            #{filePath},
            #{originalFileName},
            #{realFileName},
            #{fileExtension},
            #{fileSize},
            #{createdId},
            #{createdAt}
        )
    </insert>

</mapper>